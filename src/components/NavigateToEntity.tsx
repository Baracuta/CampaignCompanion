import { Entity } from "../types/Entity"
// This will be used in the AvatarLists and SearchBar
// Set it up so that it opens the displaything with the passed entity. Should be relatively straightforward.
type NavProps={
    entity:Entity;
}

function NavigateToEntity(props:NavProps){

}


export default NavigateToEntity